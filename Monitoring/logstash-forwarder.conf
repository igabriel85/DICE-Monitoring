{
  "network": {
    "servers": [ "10.211.55.185:5000" ], #IP and port same as in the logstash server configuration
    "timeout": 15,
    "ssl ca": "/opt/certs/logstash-forwarder.crt" #location of certificate copied from logstash server
  },

  "files": [
    {
      "paths": [
        "/var/log/syslog",
        "/var/log/auth.log"
      ],

      "fields": { "type": "syslog" }
    }
    ]
}